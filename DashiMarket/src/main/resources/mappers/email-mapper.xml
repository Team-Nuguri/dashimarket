<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.og.project.member.model.dao.EmailMapper">

   <update id="updateAuthKey">
      UPDATE AUTH_TABLE SET  
      CODE = #{authKey},
      -- 🚨 CREATE_TIME을 CREATE_DATE로 수정 
      CREATE_DATE = SYSDATE 
      WHERE EMAIL = #{email}
   </update>
   
   <insert id="insertAuthKey">
      INSERT INTO AUTH_TABLE VALUES(SEQ_AUTH_KEY_NO.NEXTVAL, #{authKey}, #{email}, DEFAULT)
   </insert>
   
	<select id="checkAuthKey" resultType="_int">
	    SELECT COUNT(*) 
	    FROM AUTH_TABLE  
	    WHERE EMAIL = #{email}
	    AND CODE = #{inputKey}
	    
	    AND CREATE_DATE + (5 / 24 / 60) > SYSDATE
	</select>

</mapper>