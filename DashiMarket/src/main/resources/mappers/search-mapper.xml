<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.og.project.main.mapper.MainMapper">
	
	<!-- 인트로 검색 - 제목  -->
	<select id="introSearch" resultType="map">
		SELECT * FROM(
			SELECT BOARD_NO, BOARD_TITLE, BOARD_CODE, BOARD_TYPE 
            FROM BOARD
            JOIN BOARD_TYPE USING(BOARD_CODE)
            WHERE BOARD_TITLE LIKE '%${query}%'
            AND DELETE_FL = 'N'
            ORDER BY BOARD_CODE DESC)
        <![CDATA[ WHERE ROWNUM <= 5 ]]>
	</select>
	
</mapper>